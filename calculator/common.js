// 구구단
for (start = 2; start < 10; start++) {
    for (startSecond = 1; startSecond < 10; startSecond++) {
        result = start + "x" + startSecond + "=" + start * startSecond + "<br>";
        document.write(result);
    }
}
